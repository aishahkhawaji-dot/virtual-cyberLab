# Virtual CyberLab

## Windows quick start
Double-click `START_Virtual_CyberLab.bat`. It installs the required packages and launches the app.

## Manual
```text
py -m pip install -r requirements.txt
py -m streamlit run app.py
```

`reportlab` is required only for the PDF Progress Report and is included in `requirements.txt`.
